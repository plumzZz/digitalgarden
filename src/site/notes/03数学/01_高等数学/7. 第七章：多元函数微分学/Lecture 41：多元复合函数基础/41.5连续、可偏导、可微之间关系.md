---
{"tags":[],"dg-publish":true,"permalink":"/03数学/01_高等数学/7. 第七章：多元函数微分学/Lecture 41：多元复合函数基础/41.5连续、可偏导、可微之间关系/","dgPassFrontmatter":true}
---

---
## 41.5 连续、可偏导、可微之间关系
**一元函数**
+ 图示：
	+ ![Pasted image 20240504025921.png](/img/user/01%E9%99%84%E4%BB%B6/Pasted%20image%2020240504025921.png)

**多元函数**
+ 1. 和一元相同点：
	+ （1）连续 `-x>` 可导；
	+ （2）可微 `->` 可偏导；
	+ （3）可微 `->` 连续；
	+ （4）连续 `-x>` 可导；
+ 2. 和一元不同点：
	+ 原因：
		+ 不一样的地方，都是**可偏导导致出来的**，为什么多元不行？
		+ 可偏导实际上是 $f_x(x_0,y_0)$ 和 $f_y(x_0,y_0)$ 的偏导数； 
		+ $f_x(x_0,y_0)$ `->` $f(x,y_0)$ 在 $y=y_0$ 这一条线上，函数发生变化，只和这一条线上的函数值有关； 
		+ $f_y(x_0,y_0)$ `->` $f(x_0,y)$ 在 $x=x_0$ 这一条线上，函数发生变化，只和这一条线上的函数值有关；
		+ 而连续 `->` 其是一个重极限，因为它要求、函数在任意方向的变化都是连续的，而偏导数只能决定在某一条线上、当前的导数是否存在，因此不能说明任意方向上是否连续；
		+ 一元的可导为什么可以：因为一元当中，可导表示了这个区域内都是可导的，因此可导 `->` 可微且连续；
	+ 概念：
		+ （1）可偏导 `-x>` 连续；
		+ （2）可偏导 `-x>` 可微；
+ 3. 和一元相比新的点：
	+ （1）偏导数连续 `->` 可微；
	+ （2）可微 `-x>` 偏导数连续；
+ 图示：
+ 可导与可微，连续
	
	![Pasted image 20250808154342.png](/img/user/01%E9%99%84%E4%BB%B6/Pasted%20image%2020250808154342.png)



# 从可微的角度来讲一元函数和多元函数是一样的概念
![Pasted image 20250808154637.png](/img/user/01%E9%99%84%E4%BB%B6/Pasted%20image%2020250808154637.png)
